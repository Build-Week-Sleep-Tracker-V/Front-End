import React, {useState} from 'react';
import axios from 'axios';

const Login = () => {
    const [data, setData] = useState({
        firstName:''
        // fill in properties needed for log in form that will be used for state
    })

    // 
    
    return(
        <div>
            <h1>Login</h1>
        </div>
    )
}

export default SleepInput